module.exports = class Stack {
    constructor() {
        this.s = [];
    }

    push(coop) {
        s.push(coop);
    }

    pop() {
        let subject = s.pop();

        //eval the neighbors if changed
    }
}